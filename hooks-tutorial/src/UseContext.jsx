import React from 'react'

const UseContext = () => {
  return (
    //we can wrap the values that are passed as props, into a single context in the parent so that it can be accessed by other components without props
    //createContext,createContext
    <div>UseContext</div>
  )
}

export default UseContext